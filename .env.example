# Copy to .env for local development. Never commit .env.
# Every variable is prefixed APP_ so it cannot collide with anything else
# on the host. An unknown APP_* variable is rejected at startup as a typo.

APP_ENVIRONMENT=local
APP_LOG_LEVEL=INFO
APP_LOG_FORMAT=console
APP_HOST=0.0.0.0
APP_PORT=8000
